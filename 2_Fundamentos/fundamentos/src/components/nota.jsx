import React from 'react'

const nota = (props) => {
  return (
    <div>
        <legend>Informe a Nota:</legend>
        <input type="text" value='' onChange='' />

    </div>
  )
}

export default nota